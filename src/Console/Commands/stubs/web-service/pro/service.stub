<?php

namespace App\Services;

use App\Repositories\{{{singular_uc}}}Repository;
use Illuminate\Database\Eloquent\ModelNotFoundException;

class {{{singular_uc}}}Service
{

    public function __construct(
        protected {{{singular_uc}}}Repository ${{{singular}}}Repository
    ) {
        //
    }

    public function plucks()
    {
        return $this->{{{singular}}}Repository->plucks();
    }

    public function list(array $filters)
    {
        return $this->{{{singular}}}Repository->filter($filters);
    }

    public function create(array $data)
    {
        return $this->{{{singular}}}Repository->create($data);
    }

    public function read($id, array $filters = [])
    {
        ${{{singular}}} = $this->{{{singular}}}Repository->newObject();
        ${{{singular}}} = ${{{singular}}}->whereId($id);
        ${{{singular}}} = $this->{{{singular}}}Repository
                ->filter($filters, ${{{singular}}})
                ->first();

        return ${{{singular}}} ?? throw new ModelNotFoundException();
    }

    public function update($id, array $data)
    {
        return $this->{{{singular}}}Repository->update($id, $data);
    }

    public function delete($id)
    {
        return $this->{{{singular}}}Repository->delete($id);
    }
}
